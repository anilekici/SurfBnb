<div class="show-container">
  <div class="show-text">
    <h2> <%= @surfboard.model %> </h2>
  </div>

  <div class="show-text">
    <p> Description: <%= @surfboard.description %> </p>
    <p><%= @surfboard.size %>, €<%= @surfboard.price %>/day </p>
  </div>

  <div class="flex">
    <h2 class= "text-center"><%= link_to "Book Now!", new_surfboard_booking_path(@surfboard), class: "black-btn" %></h2>
  </div>

    <div class="show-image">
      <% if @surfboard.photo.attached? %>
        <%= image_tag @surfboard.photo %>
      <% else %>
        <%= image_tag 'ART00050817001.jpg' %>
      <% end %>
    </div>

</div>
